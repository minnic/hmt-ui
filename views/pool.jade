label DB Pool
input.form-control(ng-model='poolName')
ul.dropdown-menu(ng-class='{active: isActive}')
  li(ng-repeat='pool in pools')
    a(href='#' onclick='return false;' ng-click='setPoolName($index)') 
      | {{ pool.jndi }}
      br
      | {{ '(' + pool.ip + ':' + pool.port + ')' }}
